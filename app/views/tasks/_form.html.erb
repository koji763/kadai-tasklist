<%= form_with(model: @task) do |f| %>
  <%= render "error_messages", task: f.object %> 

  <%= f.label :content, "タスク" %>
  <%= f.text_field :content %>
  
  <%= f.label :status, "状況" %>
  <%= f.text_field :status %>
  
  <%= f.submit "作成" %>
<% end %>